export type CategoryType = {
    id: number
    category: {
        categoryName: string
    }
    slug: string
    mainImage: {
        url: string
    }
}

export type CategoryTypeVariant = {
    id: number
    categoryName: string
    slug: string
    mainImage: {
        url: string
    }
}